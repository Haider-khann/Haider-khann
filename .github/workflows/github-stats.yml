name: Generate GitHub Stats

on:
schedule:
- cron: "0 0 * * *"
workflow_dispatch:

jobs:
stats:
runs-on: ubuntu-latest

```
steps:
  - name: Generate GitHub Stats
    uses: anuraghazra/github-readme-stats@master
    env:
      PAT_1: ${{ secrets.PAT_1 }}
    with:
      username: Haider-Khann
```
